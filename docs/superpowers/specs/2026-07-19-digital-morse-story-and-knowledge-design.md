# 数字摩斯项目展示与知识库设计

日期：2026-07-19
状态：已落地本地展示，待上线授权
执行合同：`STAGED / STANDARD / LOCAL`

## 1. 展示合同

数字摩斯沿用作品页统一的“折叠项目行 + 原位展开详情”模式，公开文案以招聘方、潜在客户和同行的快速理解为目标。

折叠层只显示：

- 项目名称：数字摩斯
- 两行简介：嵌入个人作品集的 AI 数字分身系统，通过自由对话、JD 匹配和需求初诊，帮助访客快速了解项目与能力，并获得带来源的可追溯回答。
- 五个能力短词：三类对话工作流、BGE + pgvector、可追溯来源、受控联网、停止与恢复。
- 真实状态：唯一开发者 · 已上线 · 持续完善中。
- 主图：数字摩斯对话主界面，使用示例会话；角标为“产品界面 · 示例会话”。

展开详情固定为五段：

1. 项目简介
2. 核心能力
3. 系统架构
4. 我的技术实现
5. 技术栈

完整文案以 `content/drafts/system-digital-morse.md` 为确认源，并同步到 `content/site-content.json` 的 `projects.digital-morse.details`。

## 2. 内容口径

项目定位为嵌入个人作品集的 AI 数字分身系统，面向招聘方、潜在客户和同行。系统公开呈现自由对话、JD 匹配、需求初诊、RAG、受控联网和可靠对话能力。

“唯一开发者”只描述全部技术实现由摩斯完成。需求判断、产品方向和部分创意吸收真实业务沟通输入，不扩写为独自提出全部业务和产品设计。

生成模型与嵌入模型的具体名称、版本不写入长期作品集文案；生成链路使用模型无关的 Provider / Responses 表述，向量检索保留 BGE Embeddings 能力族。技术栈保留框架、数据系统、检索实现和工程交付能力。

未来方向单独写明语音与视频表达、经授权且可撤销的长期记忆、人工审核的知识更新工作流。

## 3. 知识库结构

六个主题继续使用稳定文档 ID，并统一链接 `/works#digital-morse`：

1. `project-digital-morse-overview`：项目定位与价值。
2. `project-digital-morse-workflows`：使用流程。
3. `project-digital-morse-knowledge`：核心架构。
4. `project-digital-morse-reliability`：关键技术实现。
5. `project-digital-morse-role`：个人技术贡献。
6. `project-digital-morse-roadmap`：未来方向。

`content/rag-eval.json` 的 gold 问法与六个主题语义对应，同时保留 `project-digital-morse` 作为整体能力文档。

## 4. 入口与媒体

“问数字摩斯”只打开现有站内对话、切回自由对话并预填获批问题，不自动发送。CTA 位于五段详情之后，外部 GitHub 入口与其并列。

主图沿用 `public/works/digital-morse/digital-morse-main-local-2026-07-19.png`。作品卡片对数字摩斯使用右上角短角标，避免遮挡示例界面入口；其他项目沿用共享媒体角标规则。

## 5. 验收合同

- 折叠层显示两行简介、5 个短词、状态和短角标。
- 展开层按五段顺序渲染，不出现旧六段审计标题或媒体元数据。
- 数字摩斯六个知识主题可独立抽取，并与 `/works#digital-morse` 关联。
- “问数字摩斯”保持原位预填、不自动发送。
- 1440×900 与 390×844 均完成主图加载、展开、Hash、CTA、无横向溢出和错误列表检查。
- 本地测试、生产构建和数字摩斯双宽浏览器 smoke 通过后，才进入上线授权评估。

## 6. 非目标

- 不改数字摩斯对话产品逻辑，不新增模型或 Provider。
- 实现与验收阶段不执行知识摄取、真实 Provider 调用、数据库写入或部署；源码同步仅在单独获得 closeout 授权后执行。
- 不替其他项目补写详情，不把未来方向写成已完成能力。
