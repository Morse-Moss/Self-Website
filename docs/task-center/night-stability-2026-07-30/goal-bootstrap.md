# Night Stability Goal Bootstrap

1. Read `docs/task-center/run-state.md` and use its single `current_pointer`.
2. Load `docs/task-center/night-stability-2026-07-30/task.md`.
3. Load only the matching contract under `stage-contracts/`.
4. Read the last entry in `progress-ledger.md` and all open entries in `failure-register.md`.
5. Return execution to `morse-development-mode`; approvals remain owned by `morse-dev-sop`.
6. Stop the current wave on any zero-tolerance answer, metadata, charge, privacy or production-health failure.
7. Do not enable Skills, tool Agent, automatic search or a public percentage rollout.
